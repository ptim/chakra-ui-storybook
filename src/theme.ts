import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  semanticTokens: {
    colors: {
      "chakra-body-text": {
        // _light: "purple.800",
        // _dark: "pink.100"
      },
      "chakra-body-bg": {
        // _light: "pink.100",
        // _dark: "purple.800"
      }
    }
  }
});
